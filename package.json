{
  "name": "cosmo-gerson---ssg-sci-fi-latino",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo \"This project is a static site. Run 'npm run generate-pages' then 'npm run preview-build' to preview.\"",
    "build": "vite build",
    "clean": "rimraf build",
    "build:all": "npm run clean && npm run generate-pages",
    "build:css": "tailwindcss -i ./src/styles/globals.css -o ./build/styles.css --minify",
    "generate-pages": "node scripts/build.cjs",
    "preview-build": "npx serve build",
    "generate-and-preview": "npm run generate-pages && npx serve build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "autoprefixer": "^10.4.23",
    "markdown-it": "^14.1.0",
    "postcss": "^8.5.6",
    "rimraf": "^5.0.0",
    "serve": "^14.2.0",
    "tailwindcss": "^3.4.19",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
